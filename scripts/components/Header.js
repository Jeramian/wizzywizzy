import React from "react";

export default class Header extends React.Component
{
    render()
    {
        const name ="Jeremy";
        return(
            <header>Header</header>
        );
    }
}